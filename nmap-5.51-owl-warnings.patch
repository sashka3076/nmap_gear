--- nmap/nping/PacketElement.h
+++ nmap/nping/PacketElement.h
@@ -108,7 +108,7 @@ class PacketElement {
   public:
 
     PacketElement();
-    ~PacketElement();
+    virtual ~PacketElement();
 
     /** This function MUST be overwritten on ANY class that inherits from
       *  this one. Otherwise getBinaryBuffer will fail */
